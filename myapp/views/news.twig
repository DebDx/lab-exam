

{% extends 'layout.twig' %}

{% block body %}
  <h1>{{title}}</h1>
  {% for rec in records %}
  <p>{{ rec.regno }}</p>
  <p>{{ rec.date }}</p>
  
  <p>{{ rec.title }}</p>
  <p>{{ rec.data }}</p>
  <p>{{ rec.status }}</p>
  {% endfor %}